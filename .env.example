# ArchiSuite — variáveis de ambiente
# Copie para .env e preencha os valores.

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Banco — local na VM (Postgres no sistema, Docker ou K8s — ver docs/VM-SETUP.md)
DATABASE_URL=postgresql://archisuite:arhsuite123@@localhost:5432/archisuite

# Arquivos — pasta local na VM
UPLOADS_DIR=./uploads
# ou UPLOADS_DIR=/var/archisuite/uploads

# IA — Google Gemini (obter em https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=AIzaSyAHnpQApoWZUmZE4lLIA-MIaXH-Hhcly5M
# Opcional: modelo de imagem. Padrão = gemini-2.5-flash-image (500/dia).
# Pro (20/dia): menos artefatos e layout mais consistente entre visões.
# GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview
